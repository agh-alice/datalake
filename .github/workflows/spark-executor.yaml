name: build-spark-executor
on:
  workflow_dispatch:
  push:
    branches: [master]
    paths: ['images/spark-executor/**', 'src/spark/**', '.github/workflows/spark-executor.yaml']
permissions:
  contents: read
  packages: write
jobs:
  build-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: images/spark-executor/Dockerfile
          push: true
          # NOT ghcr.io/agh-alice/spark-executor: that package predates this CI
          # (hand-pushed) and is not linked to this repo, so GITHUB_TOKEN gets
          # write_package denied. A repo-scoped name is auto-linked with write.
          tags: ghcr.io/agh-alice/datalake/spark-executor:${{ github.sha }}
